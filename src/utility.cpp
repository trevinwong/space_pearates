#include "utility.hpp"

void printVec2(string name, vec2 vec)
{
	cout << name << ": " << to_string(vec) << endl;
}

void printVec3(string name, vec3 vec)
{
	cout << name << ": " << to_string(vec) << endl;
}
